facing  = 1;   // 0 either side, 1 only when player faces right, -1 only when faces left
hang_dx = -5;  // hands offset from marker when facing right (neg = left)
hang_dy = -5;  // hands vertical offset (neg = above)
pull_dx = 6;  // stand X offset from marker (pos = to the right)
pull_dy = -12; // stand Y offset (neg = up)
autopull = true;
